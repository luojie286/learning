#include<iostream>
#include<vector>
#include<algorithm>

using namespace std;

void solve(){
    int N,n1,n2;
    cin>>N>>n1>>n2;
    cin.ignore();
    vector<vector<int>>a(n1);
    vector<vector<int>>b(n2);
    for(int i=0;i<n1;i++){
        string s;
        getline(cin,s);
        vector<string>t;
        for(char x:s){
            if()
        }
    }
}

int main() {
    int t;
    cin>>t;
    while(t--)
    solve();
    return 0;
}