# 操作系统学习笔记 · 总览与速记

> 整理方式：按主题重构，每节附「易错点 / 速记口诀 / 自测问题」便于复习
> 旧笔记：原始按日期记录的碎片已归档到 `_archive/`，需要回查可翻那里

---

## 一、主题索引

| 编号 | 主题 | 文件 | 核心问题 |
|------|------|------|----------|
| 01 | 操作系统基础概念 | `01-操作系统基础概念.md` | 进程有哪几个状态？特权指令是什么？共享内存和消息传递怎么选？ |
| 02 | 从源码到可执行文件 | `02-从源码到可执行文件.md` | `gcc` 一条命令背后有哪四步？`undefined reference` 属于哪个阶段？ |

> 📎 「线程、进程与锁」的内容**不在这里**——那份笔记（原 `os/20260914C.md`）本质是 Java 多线程知识，已迁入 → `java/14-多线程与锁.md`

---

## 二、核心口诀速记卡

1. **进程三态**：就绪等 CPU、运行占 CPU、阻塞等事件
2. **状态迁移**：阻塞被唤醒**只能回就绪**，不能直接上 CPU
3. **特权指令**：只在内核态执行（IO 指令、改 PSW）；用户程序进内核的唯一合法入口 = **系统调用**
4. **IPC 二选一**：共享内存**最快但需自己同步、不能跨机器**；消息传递**要拷贝但可跨机器**（→ socket 的起点）
5. **栈帧**：参数 + 返回地址 + 旧基址 + 局部变量；递归太深必爆栈
6. **编译四步**：预处理（`.i`）→ 编译（`.s`）→ 汇编（`.o`）→ 链接（可执行）
7. **链接错误**：`undefined reference` = 链接期找不到实现，不是语法错

---

## 三、复习路径建议

- **首次过**：按编号顺序读，两个文件合计约 15 分钟
- **1 天后**：用「自测问题」自查，卡壳回去看对应节
- **1 周后**：只看「核心口诀速记卡」，能复述 7 条说明掌握
- **常翻**：编译报 `undefined reference` 时回 `02` 查原因

---

## 四、整份笔记的元数据

- 整理日期：2026-09-14
- 覆盖的原始笔记：
  - `20260902.md`：共享内存、消息传递、特权指令、等待 IO 指令、状态迁移、stack frame、状态机（**仅为关键词碎片，正文按标准教材补全**）→ `01`
  - `20260905.md`：CSAPP 编译链接课堂速记 → `02`
  - `20260914C.md`：线程与锁 → **已迁出至** `java/14-多线程与锁.md`
- 缺失主题（需要后续补）：

| 主题 | 备注 |
|------|------|
| 进程调度算法 | FCFS / SJF / 时间片轮转 / 优先级 |
| 同步与互斥 | 信号量、管程、经典同步问题（生产者-消费者等） |
| 死锁 | 四个必要条件、银行家算法（Java 视角部分见 `java/14-多线程与锁.md`） |
| 内存管理 | 分页 / 分段 / 虚拟内存 / 页面置换算法 |
| 文件系统 | inode、目录结构、磁盘调度 |
