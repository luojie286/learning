# 2026-09-16 工作日志

## java 增量整理（4 篇新笔记 → 5 个新主题 + 2 处并入）

**输入**：java/20260916A.md（20KB，线程池/Future/异步）、20260916B1.md（9KB，volatile）、20260916B2.md（25KB，类加载+包导入+字节码/栈帧+原子性）、20260916B3.md（17KB，JMM）

### 产出

| 新文件 | 来源 | 处理要点 |
|---|---|---|
| `19-JMM与内存可见性.md` | B3 | 三大特性、主内存/工作内存、重排序与 as-if-serial、happens-before 六条规则、**data/ready 完整推导链**、JMM vs JVM 内存区域 |
| `20-volatile.md` | B1 + B3 的 volatile 部分 | 去重合并；可见性/有序性/不保复合原子、vs synchronized 表、判断原则 |
| `21-线程池与异步编程.md` | A | 线程池价值、ExecutorService、任务/线程分离、Runnable/Callable/Future、同步 vs 异步、CompletableFuture 全套（thenApply/Accept/Run/Combine/allOf/exceptionally）、commonPool |
| `22-类加载机制.md` | B2 §1-2 | 五阶段、准备阶段零值 vs `static final` 常量例外、`<clinit>` 按源码顺序、父先于子、static{} vs {} vs 构造 |
| `23-JVM字节码与栈帧.md` | B2 §4-16 | javap、常见字节码、栈式 vs 寄存器式、栈帧四部分、slot（this / long 占 2）、StackOverflowError、字节码条数≠原子性 |

**并入既有篇**：Java 文件结构与 import 细节 → `02-包与导入.md`；「多个原子操作组合 ≠ 原子（check-then-act）」→ `17-JUC并发工具.md` 新增小节

**其他**：`00-总览与速记.md` 索引/口诀卡扩至 23 条并加阅读依赖说明；根 README 同步（目录导航、java 明细、待补清单、整理记录）；原 4 个文件归档至 `java/_archive/`；补完了此前待补清单里的「JVM 类加载机制」（源自 `笔记1.md` 的一行标记）

### 关键决策

- **JMM 与 volatile 拆成两篇**而非合并：JMM 是模型（happens-before 规则），volatile 是模型下的一个工具，合起来会超 10KB 且自测重点不同；两篇互挂交叉引用、内容去重（count++ 例子只在 20 讲原理，19 只做推导链）
- **B2 是「一文件多主题」的典型**（类加载 + 包导入 + 字节码 + 原子性），按主题拆到 4 个去向，其中包导入并入 02、原子性新点并入 17，避免重复文件
- 22/23 归入 `java/` 而非新建 `jvm/` 目录：README 待补清单原本就把 JVM 记在 java 项下，编号延续（19–23）比开新目录更利于当前复习

### git

- commit + push（`GIT_TERMINAL_PROMPT=0 git -c credential.helper= -c credential.helper=store push origin main`），沿用 09-15 验证的 GCM 绕过方案
