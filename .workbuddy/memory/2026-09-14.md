# 2026-09-14 工作日志

## 整理 `E:\learning` 全工作区学习笔记

**范围**：工作区下所有 md（9 个学科目录、33 个文件）
**方式**：按主题重构 + 三件套复习钩子（📌易错点 / 🧠速记口诀 / ✅自测问题），沿用上次 `java/` 的规范
**用户选择**：范围=整个 learning 工作区；方式=按主题重构 + 复习钩子

### 完成内容

1. **Java 新增 6 篇主题（11–16）**
   - `11-集合框架.md` ← `20260914上午.md`（33KB，极啰嗦，压到约 1/4）+ `java_chat_study_notes.md` 的 List 部分
   - `12-泛型.md` ← `java_chat_study_notes.md` 泛型/泛型反射部分，**补全 PECS**（原文只覆盖 `? extends` 一侧）
   - `13-IO与资源管理.md` ← `20260914B.md` 第 1 节
   - `14-多线程与锁.md` ← `os/20260914C.md`（**归类错误，内容实为 Java**）+ `20260914B.md` 第 6 节
   - `15-日期时间与正则.md` ← `20260914B.md` 第 2、4 节
   - `16-单元测试与安全基础.md` ← `20260914B.md` 第 3、5 节
   - 更新 `java/00-总览与速记.md`：索引扩到 16 篇、口诀卡扩到 16 条、元数据补来源与缺失主题

2. **`os/`**：2 个关键词碎片扩写为 `01-操作系统基础概念.md`、`02-从源码到可执行文件.md`，加 `00-总览`

3. **`计算机网络/`**：`t.md` 只有教材大纲 → `01-网络概述与体系结构.md`（补全 OSI/TCP-IP 分层），加 `00-总览`

4. **`数电/` `嵌入式学习/` `cmake/`**：规范化 + 补钩子。**修正了 cmake 的三处错误**——用了 `//` 注释（CMake 只认 `#`）、`VERSION_ 3.10` 多下划线、`my_poject` 拼写错误

5. **新增根 `README.md`**：全工作区索引 + 待补清单 + 零散留痕 + 整理记录

6. **归档**：15 个原始文件移入各目录 `_archive/`，**无一删除**

### 关键决策

- **扩写内容必须标注来源**：`os/01`、`os/02`、`计算机网络/01` 的原始笔记只有关键词或大纲标题，正文是按标准教材补全的。在这三篇开头 ⚠️ 与 footer 都明确标注「非原始记录，以课本/课件为准」，根 README 再单独提示一次——避免日后误当成自己记的内容。
- **跨目录归属纠正**：`os/20260914C.md`（线程与锁）内容属 Java，归档到 `java/_archive/` 而非 `os/_archive/`，并在 `os/00-总览` 写明去向。
- **空文件与单行文件不丢信息**：`QT学习/` 两个 0B 文件归档；单行文件（Python 的 Streamlit 疑问、「框架：提高效率」、「jvm 的类加载机制」）的信息记入根 README「零散留痕」区。

### 教训（工具层面）

- **用 Edit 改写大段中文 Markdown 时出现过内容丢失**：首次编辑 `java/00-总览与速记.md`，替换索引表之后「## 二、核心口诀速记卡」整节消失，紧接着的 Edit 报 `String to replace not found`。最终用 Write 整体重写才修复。
  → **经验：对中文 Markdown 做结构性改动（增删整节）时，直接用 Write 整体重写更稳，不要依赖连续多次 Edit。**
